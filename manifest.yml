---
applications:
- name: mongo-express
  host: mongo-express
  memory: 128M
  services:
    - db-mongo
  env:
    NODE_ENV: cloud-foundry
    ME_CONFIG_BASICAUTH_USERNAME: admin
    ME_CONFIG_BASICAUTH_PASSWORD: password
